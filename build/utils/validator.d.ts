/**
 * Validates a sudoku grid based on the sudoku rules
 * @param grid the grid to validate
 * @returns true if the grid is valid, false otherwise
 */
export declare const validateSudokuGrid: (grid: number[][]) => boolean;
